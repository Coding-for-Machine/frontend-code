// src/types/settings.ts (or in Playground.tsx file directly)
export interface ISettings {
    settingsModalIsOpen: boolean;
    dropdownIsOpen: boolean;
    fontSize: string;
  }
  
  // Then import it into SettingsModal.tsx
  